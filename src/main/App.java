package main;

import main.transitionframes.GridSizeFrame;

public class App {
    public static void main(String[] args) {
        GridSizeFrame gridSize = new GridSizeFrame();
        gridSize.setVisible(true);
        gridSize.setLocationRelativeTo(null);
    }
}
